name: Android Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: kivy/buildozer

    steps:
    - name: Build with buildozer
      run: buildozer android debug 
