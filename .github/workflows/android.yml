name: Android Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: kivy/buildozer

    steps:
      - uses: actions/checkout@v2
      - name: Build with buildozer
      - run: buildozer android debug 
